//
//  ContentView.swift
//  LOTRConverter18
//
//  Created by Bilyana Aneva on 5.10.24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .foregroundColor(Color.green)
                .imageScale(.large)
                .foregroundStyle(.fill)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
